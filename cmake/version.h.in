// Auto-generated version information - DO NOT EDIT
// Generated by CMake at configure time

#pragma once

namespace orpheus {
namespace version {

// Semantic version components
constexpr int MAJOR = @PROJECT_VERSION_MAJOR@;
constexpr int MINOR = @PROJECT_VERSION_MINOR@;
constexpr int PATCH = @PROJECT_VERSION_PATCH@;

// Aliases for consistency
constexpr int VERSION_MAJOR = MAJOR;
constexpr int VERSION_MINOR = MINOR;
constexpr int VERSION_PATCH = PATCH;

// Version strings
constexpr const char* VERSION = "@PROJECT_VERSION@";
constexpr const char* VERSION_FULL = "@ORPHEUS_VERSION_FULL@";

// Git information
constexpr const char* GIT_HASH = "@ORPHEUS_GIT_HASH@";
constexpr const char* GIT_HASH_SHORT = "@ORPHEUS_GIT_HASH_SHORT@";
constexpr const char* GIT_BRANCH = "@ORPHEUS_GIT_BRANCH@";
constexpr bool GIT_DIRTY = @ORPHEUS_GIT_DIRTY@;

// Build information
constexpr const char* BUILD_TIMESTAMP = "@ORPHEUS_BUILD_TIMESTAMP@";
constexpr const char* BUILD_DATE = "@ORPHEUS_BUILD_DATE@";
constexpr const char* BUILD_TYPE = "@CMAKE_BUILD_TYPE@";

// Platform
#if defined(PLATFORM_WINDOWS)
constexpr const char* PLATFORM = "Windows";
#elif defined(PLATFORM_LINUX)
constexpr const char* PLATFORM = "Linux";
#elif defined(PLATFORM_MACOS)
constexpr const char* PLATFORM = "macOS";
#else
constexpr const char* PLATFORM = "Unknown";
#endif

// Compiler
#if defined(_MSC_VER)
constexpr const char* COMPILER = "MSVC";
constexpr int COMPILER_VERSION = _MSC_VER;
#elif defined(__clang__)
constexpr const char* COMPILER = "Clang";
constexpr int COMPILER_VERSION = __clang_major__ * 100 + __clang_minor__;
#elif defined(__GNUC__)
constexpr const char* COMPILER = "GCC";
constexpr int COMPILER_VERSION = __GNUC__ * 100 + __GNUC_MINOR__;
#else
constexpr const char* COMPILER = "Unknown";
constexpr int COMPILER_VERSION = 0;
#endif

// Helper functions
inline const char* GetVersionString() {
    return VERSION_FULL;
}

inline const char* GetFullVersionInfo() {
    static char buffer[256];
    snprintf(buffer, sizeof(buffer),
        "%s (%s) - %s %s",
        VERSION_FULL, GIT_HASH_SHORT, PLATFORM, BUILD_TYPE);
    return buffer;
}

inline const char* GetBuildInfo() {
    static char buffer[512];
    snprintf(buffer, sizeof(buffer),
        "Version: %s\n"
        "Git: %s (%s)%s\n"
        "Built: %s\n"
        "Platform: %s\n"
        "Compiler: %s\n"
        "Build Type: %s",
        VERSION_FULL,
        GIT_HASH_SHORT, GIT_BRANCH, GIT_DIRTY ? " [dirty]" : "",
        BUILD_DATE,
        PLATFORM,
        COMPILER,
        BUILD_TYPE);
    return buffer;
}

} // namespace version
} // namespace orpheus
